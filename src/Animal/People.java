package Animal;

public class People {

    String name;
    String lastName;
    int age;

    Cat cat;

    @Override
    public String toString() {
        return "People[name " + name + ",lastname " + lastName + "age" + age + "cat" + cat + "]" ;
    }
}
